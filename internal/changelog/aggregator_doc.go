// Package changelog provides primitives for fetching, parsing, and
// processing dependency changelog entries.
//
// Aggregator computes summary statistics over a collection of Entry values,
// including per-dependency counts, per-label counts, and the number of
// highlighted entries. Use TopDependencies to retrieve the most active
// dependencies in descending order of entry volume.
package changelog
